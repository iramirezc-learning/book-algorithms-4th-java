if [ -z "$1" ]
then
  javac -d $MY_CLASSPATH -cp $CLASSPATH ./adts/*.java
  javac -d $MY_CLASSPATH -cp $CLASSPATH ./algorithms/*.java
  javac -d $MY_CLASSPATH -cp $CLASSPATH ./examples/*.java
else
  javac -d $MY_CLASSPATH -cp $CLASSPATH $1
fi
